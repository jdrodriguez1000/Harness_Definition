Inicia el FORGE Discovery Harness en el proyecto actual.

## Instruccion

Invocar el agente `discovery-governor` en modo INIT pasando este mensaje:

```
MODO: INIT
```

No agregues ningun texto antes ni despues de invocar el agente.
El discovery-governor tomara el control del flujo desde ese punto.
